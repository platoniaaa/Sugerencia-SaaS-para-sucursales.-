[project]
name = "sugerido-api"
version = "0.1.0"
description = "Backend de la plataforma Sugerido de Compras (Fase 0)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
